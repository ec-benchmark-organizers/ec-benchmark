import matplotlib.pyplot as plt


def plot_contour(points):
    """
    Plots the environmental contour.
    ----------
    points : Class of points,
        Description what the points are.
    """

	
def compute_metric(points)
    """
    Computes something.
    ----------
    points : Class of points,
        Description what the points are.
    """

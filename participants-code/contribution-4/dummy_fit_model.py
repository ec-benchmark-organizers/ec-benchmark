import numpy as np
import scipy.stats as sts


def fit_model(dataset):
    """
    Fits the model to the dataset.
    ----------
    dataset : Class of dataset,
        Description what the dataset is.
    """
